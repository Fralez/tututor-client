import React, { useState } from "react";
import styled from "styled-components";
import tw from "tailwind.macro";

const ChatLog = () => { return (<Log>

</Log>)};

export default ChatLog;

const Log = styled.div`
    ${tw``}
    height: 200px;
    width: 200px;
    background: #e2e8f0; 
`;